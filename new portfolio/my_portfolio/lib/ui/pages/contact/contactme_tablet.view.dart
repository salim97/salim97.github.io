part of contactme_view;

class _ContactMeTablet extends StatelessWidget {
  final ThemeData theme;

  _ContactMeTablet(this.theme);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text('ProjectsTablet')),
    );
  }
}
